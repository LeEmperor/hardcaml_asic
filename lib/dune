; Initial hardcaml_asic package. Keep portable resources, technology bindings,
; and project/flow support behind clear module boundaries in one package;
; split packages when dependencies or distribution require it. No technology
; backend is implemented yet. See docs/architecture.md section 2.

(library
 (name hardcaml_asic)
 (libraries core hardcaml yojson cryptokit unix)
 (preprocess
  (pps ppx_jane)))
